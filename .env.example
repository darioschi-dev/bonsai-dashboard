# ------------------------------------------------------------------------------
# 🌱 Bonsai IoT Dashboard — Server OTA + MQTT Backend
# ------------------------------------------------------------------------------

# Porta HTTP del server Express
PORT=8081

# Interfaccia su cui ascoltare (0.0.0.0 = tutte)
HOST=0.0.0.0

# URL base pubblico del server
# - In sviluppo locale: http://192.168.1.240:8081
# - In produzione: https://bonsai-iot-update.darioschiavano.it
BASE_URL=http://192.168.1.240:8081

# Indirizzo del broker MQTT (può essere mqtt://localhost o IP)
MQTT_URL=mqtt://192.168.1.240:1883

# Host di update usato per filtrare richieste non OTA
# (in genere coincide col dominio pubblico del server OTA)
UPDATE_HOST=bonsai-iot-update.darioschiavano.it

# Token opzionale per autenticare gli upload OTA (GitHub Actions)
# Se lasciato vuoto, nessuna autenticazione viene richiesta.
OTA_TOKEN=

# Livello di log (info | debug)
LOG_LEVEL=info
